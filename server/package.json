{
  "name": "osu-trainer-web-server",
  "version": "1.0.0",
  "description": "Server-side audio processor for osu! trainer web - handles FFmpeg processing so the browser doesn't have to",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^8.5.2",
    "ffmpeg-static": "^5.2.0",
    "fluent-ffmpeg": "^2.1.3",
    "multer": "^1.4.5-lts.1"
  }
}
